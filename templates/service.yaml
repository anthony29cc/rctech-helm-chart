apiVersion: v1
kind: Service
metadata:
  name: {{ include "rctech-web.fullname" . }}
  labels:
    {{- include "rctech-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "rctech-web.selectorLabels" . | nindent 4 }}
